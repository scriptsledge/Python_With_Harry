# Tuple containing numbers with multiple zeroes
a = (7, 0, 8, 0, 0, 9)

# count(value): counts occurrences of the specified value (0 in this case)
count = a.count(0)

# Print the number of zeroes present in the tuple
print(f"There are {count} zeroes present in the tuple.")
