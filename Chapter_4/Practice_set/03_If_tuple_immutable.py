# Check if tuples are immutable
alpha = ('a', 'b', 'c', 'd')

# Attempting to modify a tuple element will raise a TypeError
# Uncommenting the line below will demonstrate immutability
# alpha[1] = "banana"  # This will cause an error
