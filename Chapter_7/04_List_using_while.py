lisst = [int(False), 1, "2", 3.00, "Four", 'V']
length = 0

# Loop through the list until reaching its end
while (length < len(lisst)):
    print(lisst[length])  # Print current element
    length += 1  # Move to the next element
