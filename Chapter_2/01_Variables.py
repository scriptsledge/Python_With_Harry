# A variable is the name given to a memory location in a program
a = 1
b = 2
# an identifier is the name you use to reference a variable, and the variable itself is the container that holds a value. Identifiers can also name other things, while variables are specifically for storing data
print(a+b)